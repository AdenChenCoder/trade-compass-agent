import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tradecompass.mobile.dev',
  appName: '交易罗盘',
  webDir: 'dist',
  ios: { contentInset: 'never' },
};
export default config;
