from __future__ import annotations

SERVICE_DESCRIPTION = "Trade Compass — local A-share agent (serve + scheduler + channels)"
LAUNCHD_LABEL = "com.trade-compass.serve"
SYSTEMD_UNIT_NAME = "trade-compass.service"
SERVICE_MARKER = "TRADE_COMPASS_SERVICE_MARKER=1"
SERVICE_KIND = "serve"
